[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # long lines allowed - app has long CSS/prompt f-strings by design

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
strict = false
